const products={bifold:{title:'Bifold doors',price:'£2,025',use:'For wide, flexible openings',count:4},sliding:{title:'Sliding doors',price:'£3,750',use:'For larger panes and broad views',count:3},french:{title:'French doors',price:'£1,884',use:'For balanced everyday access',count:2},single:{title:'Single doors',price:'£1,091',use:'For compact external openings',count:1}};const title=document.getElementById('selector-title');const price=document.getElementById('selector-price');const use=document.getElementById('selector-use');const render=document.getElementById('hero-door');function selectProduct(key){const product=products[key];if(!product)return;title.textContent=product.title;price.textContent=product.price;use.textContent=product.use;render.dataset.count=String(product.count);Array.from(render.children).forEach((panel,index)=>{panel.classList.toggle('hidden',index>=product.count)});document.querySelectorAll('.selector-tab').forEach(tab=>{const selected=tab.dataset.product===key;tab.classList.toggle('active',selected);tab.setAttribute('aria-selected',String(selected))})}document.querySelectorAll('.selector-tab').forEach(tab=>{tab.addEventListener('click',()=>selectProduct(tab.dataset.product))});const menuButton=document.querySelector('.menu-button');const navigation=document.querySelector('.primary-nav');menuButton?.addEventListener('click',()=>{const open=navigation.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});navigation?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navigation.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));selectProduct('bifold');