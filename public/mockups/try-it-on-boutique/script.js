const menuButton=document.querySelector('.menu-button');const navigation=document.querySelector('.primary-nav');menuButton?.addEventListener('click',()=>{const open=navigation.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});navigation?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navigation.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));document.querySelectorAll('.media-stage img').forEach(image=>{const stage=image.closest('.media-stage');const markLoaded=()=>stage.classList.add('image-loaded');const markFailed=()=>stage.classList.add('image-failed');if(image.complete){if(image.naturalWidth>0)markLoaded();else markFailed()}else{image.addEventListener('load',markLoaded,{once:true});image.addEventListener('error',markFailed,{once:true})}});const brandImage=document.querySelector('.brand img');brandImage?.addEventListener('error',()=>brandImage.closest('.brand').classList.add('logo-failed'),{once:true});if(brandImage?.complete&&brandImage.naturalWidth===0)brandImage.closest('.brand').classList.add('logo-failed');const filterButtons=document.querySelectorAll('[data-filter]');const cards=document.querySelectorAll('.product-card');function applyFilter(value){filterButtons.forEach(button=>button.classList.toggle('active',button.dataset.filter===value));cards.forEach(card=>card.classList.toggle('is-hidden',value!=='all'&&card.dataset.range!==value))}filterButtons.forEach(button=>button.addEventListener('click',()=>applyFilter(button.dataset.filter)));document.querySelectorAll('[data-filter-link]').forEach(link=>link.addEventListener('click',()=>applyFilter(link.dataset.filterLink)));