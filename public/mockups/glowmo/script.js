const menuButton=document.querySelector('.menu-button');const mobileNav=document.querySelector('.mobile-nav');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));mobileNav.hidden=open});mobileNav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileNav.hidden=true;menuButton?.setAttribute('aria-expanded','false')}));
const mainImage=document.querySelector('#main-image');const mainMedia=document.querySelector('.main-media');function bindImage(img,wrap){if(!img||!wrap)return;const done=()=>wrap.classList.remove('image-loading');img.addEventListener('load',done);img.addEventListener('error',()=>wrap.classList.add('image-loading'));if(img.complete&&img.naturalWidth)done()}bindImage(mainImage,mainMedia);document.querySelectorAll('.feature-media').forEach(w=>bindImage(w.querySelector('img'),w));
document.querySelectorAll('.thumb').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.thumb').forEach(b=>b.classList.remove('active'));btn.classList.add('active');if(mainImage){mainMedia.classList.add('image-loading');mainImage.src=btn.dataset.src;mainImage.alt=btn.dataset.alt||'Glowmo Crystal Halo Lamp'}}));
const price=document.querySelector('#selected-price'),compare=document.querySelector('#selected-compare'),saving=document.querySelector('#selected-saving'),label=document.querySelector('#bundle-label');document.querySelectorAll('.bundle').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.bundle').forEach(b=>{b.classList.remove('active');b.setAttribute('aria-checked','false')});btn.classList.add('active');btn.setAttribute('aria-checked','true');price.textContent=btn.dataset.price;compare.textContent=btn.dataset.compare;saving.textContent=btn.dataset.saving;label.textContent=`${btn.dataset.pack} Pack selected`}));
