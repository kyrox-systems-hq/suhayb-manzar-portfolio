const main=document.getElementById('mainProductImage');
document.querySelectorAll('.thumb').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.thumb').forEach(b=>b.classList.remove('active'));btn.classList.add('active');main.style.opacity='.2';setTimeout(()=>{main.src=btn.dataset.src;main.style.opacity='1'},120)}));
document.querySelectorAll('.swatch').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.swatch').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.getElementById('colourName').textContent=btn.dataset.colour}));
const threshold=50;
document.querySelectorAll('.bundle').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.bundle').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const p=Number(btn.dataset.price);document.getElementById('buttonPrice').textContent='€'+p.toFixed(2);document.getElementById('shippingFigure').textContent='€'+p.toFixed(2)+' / €50';document.getElementById('meterFill').style.width=Math.min(100,p/threshold*100)+'%';const left=Math.max(0,threshold-p);document.getElementById('shippingText').textContent=left===0?'Free delivery unlocked.':'You are €'+left.toFixed(2)+' away from free delivery.'}));
